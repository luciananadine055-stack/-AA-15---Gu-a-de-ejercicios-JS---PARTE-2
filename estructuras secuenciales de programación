<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cálculo del Perímetro de un Cuadrado</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            padding: 40px;
            width: 90%;
            max-width: 500px;
        }
        
        h1 {
            color: #4facfe;
            text-align: center;
            margin-bottom: 30px;
            font-size: 28px;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #555;
        }
        
        input[type="number"] {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s ease;
        }
        
        input[type="number"]:focus {
            outline: none;
            border-color: #4facfe;
        }
        
        .btn {
            background-color: #4facfe;
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 50px;
            font-size: 16px;
            cursor: pointer;
            width: 100%;
            transition: all 0.3s ease;
            margin-top: 10px;
        }
        
        .btn:hover {
            background-color: #3a9bf7;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(79, 172, 254, 0.3);
        }
        
        .result-container {
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 25px;
            margin-top: 30px;
            border-left: 5px solid #4facfe;
            display: none;
        }
        
        .result-title {
            font-weight: bold;
            color: #4facfe;
            margin-bottom: 15px;
            font-size: 18px;
            text-align: center;
        }
        
        .calculation {
            margin: 15px 0;
            padding: 15px;
            background-color: white;
            border-radius: 8px;
            text-align: center;
        }
        
        .formula {
            font-size: 18px;
            color: #555;
            margin-bottom: 10px;
        }
        
        .result {
            font-size: 24px;
            font-weight: bold;
            color: #4facfe;
            margin-top: 10px;
        }
        
        .square-visual {
            display: flex;
            justify-content: center;
            margin: 20px 0;
        }
        
        .square {
            width: 120px;
            height: 120px;
            border: 3px solid #4facfe;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: rgba(79, 172, 254, 0.1);
        }
        
        .side-label {
            position: absolute;
            bottom: -25px;
            left: 50%;
            transform: translateX(-50%);
            font-weight: bold;
            color: #4facfe;
        }
        
        .instructions {
            background-color: #fff9e6;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 20px;
            border-left: 4px solid #ffd166;
        }
        
        .instructions h3 {
            color: #ff9e00;
            margin-bottom: 10px;
        }
        
        .code-container {
            background-color: #2d3748;
            color: #e2e8f0;
            border-radius: 8px;
            padding: 20px;
            margin-top: 30px;
            font-family: 'Courier New', monospace;
            overflow-x: auto;
        }
        
        .code-comment {
            color: #a0aec0;
        }
        
        .unit-selector {
            display: flex;
            align-items: center;
            margin-top: 10px;
        }
        
        .unit-selector label {
            margin-right: 10px;
            margin-bottom: 0;
        }
        
        .unit-selector select {
            padding: 8px;
            border: 2px solid #e0e0e0;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Cálculo del Perímetro de un Cuadrado</h1>
        
        <div class="instructions">
            <h3>Fórmula del Perímetro:</h3>
            <p>El perímetro de un cuadrado se calcula multiplicando la longitud del lado por 4:</p>
            <p style="text-align: center; font-size: 18px; margin-top: 10px;"><strong>P = 4 × L</strong></p>
            <p>Donde: <strong>P</strong> = Perímetro, <strong>L</strong> = Longitud del lado</p>
        </div>
        
        <form id="squareForm">
            <div class="form-group">
                <label for="lado">Longitud del lado del cuadrado:</label>
                <input type="number" id="lado" placeholder="Ingresa la longitud del lado" min="0" step="0.01" required>
            </div>
            
            <div class="unit-selector">
                <label for="unidad">Unidad de medida:</label>
                <select id="unidad">
                    <option value="cm">centímetros (cm)</option>
                    <option value="m">metros (m)</option>
                    <option value="km">kilómetros (km)</option>
                    <option value="in">pulgadas (in)</option>
                    <option value="ft">pies (ft)</option>
                </select>
            </div>
            
            <button type="button" class="btn" onclick="calcularPerimetro()">Calcular Perímetro</button>
        </form>
        
        <div class="result-container" id="resultContainer">
            <div class="result-title">Resultado del Cálculo</div>
            
            <div class="square-visual">
                <div class="square">
                    <span class="side-label" id="sideLabel">Lado</span>
                </div>
            </div>
            
            <div class="calculation">
                <div class="formula">P = 4 × L</div>
                <div class="formula">P = 4 × <span id="ladoValue">0</span></div>
                <div class="result">Perímetro = <span id="perimetroValue">0</span> <span id="unidadValue">cm</span></div>
            </div>
        </div>
        
        <div class="code-container">
            <pre><code>// Código JavaScript para cálculo del perímetro
<span class="code-comment">// Función para calcular el perímetro</span>
function calcularPerimetro() {
    <span class="code-comment">// Obtener el valor del lado del cuadrado</span>
    let lado = parseFloat(document.getElementById('lado').value);
    
    <span class="code-comment">// Validar que el valor sea positivo</span>
    if (lado <= 0 || isNaN(lado)) {
        alert('Por favor, ingresa un valor válido para el lado');
        return;
    }
    
    <span class="code-comment">// Calcular el perímetro (P = 4 × L)</span>
    let perimetro = 4 * lado;
    
    <span class="code-comment">// Obtener la unidad seleccionada</span>
    let unidad = document.getElementById('unidad').value;
    
    <span class="code-comment">// Mostrar el resultado</span>
    document.getElementById('ladoValue').textContent = lado;
    document.getElementById('perimetroValue').textContent = perimetro.toFixed(2);
    document.getElementById('unidadValue').textContent = unidad;
    document.getElementById('sideLabel').textContent = lado + ' ' + unidad;
    document.getElementById('resultContainer').style.display = 'block';
    
    <span class="code-comment">// Desplazarse suavemente hacia los resultados</span>
    document.getElementById('resultContainer').scrollIntoView({ 
        behavior: 'smooth' 
    });
}</code></pre>
        </div>
    </div>

    <script>
        function calcularPerimetro() {
            // Obtener el valor del lado del cuadrado
            let lado = parseFloat(document.getElementById('lado').value);
            
            // Validar que el valor sea positivo
            if (lado <= 0 || isNaN(lado)) {
                alert('Por favor, ingresa un valor válido para el lado');
                return;
            }
            
            // Calcular el perímetro (P = 4 × L)
            let perimetro = 4 * lado;
            
            // Obtener la unidad seleccionada
            let unidad = document.getElementById('unidad').value;
            
            // Mostrar el resultado
            document.getElementById('ladoValue').textContent = lado;
            document.getElementById('perimetroValue').textContent = perimetro.toFixed(2);
            document.getElementById('unidadValue').textContent = unidad;
            document.getElementById('sideLabel').textContent = lado + ' ' + unidad;
            document.getElementById('resultContainer').style.display = 'block';
            
            // Desplazarse suavemente hacia los resultados
            document.getElementById('resultContainer').scrollIntoView({ 
                behavior: 'smooth' 
            });
        }
        
        // Permitir enviar el formulario con la tecla Enter
        document.getElementById('squareForm').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                e.preventDefault();
                calcularPerimetro();
            }
        });
        
        // Enfocar el campo de entrada al cargar la página
        document.getElementById('lado').focus();
    </script>
</body>
</html>
